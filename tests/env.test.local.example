# Pytrix Test Environment Variables
# Copy this file to .env.test.local (in repository root) for real API integration tests
# The .env.test.local file should NEVER be committed to version control
#
# The .env.test.local file is for maintainers only and enables
# optional integration tests that call the real Gemini API.

# Internal test key for real Gemini API tests (maintainers only)
# Get a key from: https://aistudio.google.com/apikey
INTERNAL_GEMINI_KEY="API_KEY"
